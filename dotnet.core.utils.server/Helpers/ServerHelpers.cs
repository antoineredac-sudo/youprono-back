namespace dotnet.core.utils.server.Helpers
{
    // Ce namespace est importé dans plusieurs fichiers du projet original sans qu'on ait
    // vu, à l'analyse, de méthode précise appelée dessus. On garde une classe vide ici
    // pour que le namespace existe ; si la compilation réclame une méthode manquante,
    // on l'ajoutera ici sur le moment.
    public static class ServerHelpers
    {
    }
}
